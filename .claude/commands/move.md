---
description: What should I be doing right now
argument-hint: (no arguments)
---

# /move

Regenerate and show Your Move, per `brain/routines/your-move-refresh.md`.

Build it from the files, not from what you remember of this session: re-read `brain/goals.md`,
`brain/brain-state.md`, and the open rows in `brain/queue.json` first.

Output the locked four-block shape: top move, ranked next, rabbit-hole flag, sources. The
rabbit-hole flag is mandatory — "none" is a valid answer and must be said out loud, because an
omitted flag reads exactly like a clean bill of health.
